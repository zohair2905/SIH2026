Place the exact 449 MB `rf_baseline_model.joblib` produced by your ML team here.

Do not replace it with `random_forest_baseline_step4.joblib` unless the team confirms
that it is the same 36-feature Pipeline produced by `train_rf_baseline.py`.
